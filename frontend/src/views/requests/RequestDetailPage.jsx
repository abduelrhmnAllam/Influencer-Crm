import { useEffect,useState } from 'react';import{useParams}from'react-router-dom';import api from '../../api/client';
export default function RequestDetailPage(){const{id}=useParams();const[item,setItem]=useState(null);const[status,setStatus]=useState('');const load=()=>api.get(`/api/v1/requests/${id}`).then(({data})=>{setItem(data);setStatus(data.status)});useEffect(()=>{load()},[id]);const save=async()=>{await api.put(`/api/v1/requests/${id}`,{status});load()};if(!item)return <div className='panel'>جارٍ التحميل…</div>;return <div className='entity-page'><div className='page-heading'><div><p>{item.number}</p><h1>{item.title}</h1><span>{item.type} · {item.priority}</span></div></div><section className='panel'><h2>حالة الطلب</h2><div className='workflow-actions'><select value={status} onChange={(e)=>setStatus(e.target.value)}><option value='new'>جديد</option><option value='in_progress'>قيد التنفيذ</option><option value='waiting_client'>بانتظار العميل</option><option value='completed'>مكتمل</option><option value='cancelled'>ملغى</option></select><button onClick={save}>حفظ الحالة</button></div></section><section className='panel'><h2>وصف الطلب</h2><p>{item.brief?.description||'لا يوجد وصف'}</p></section><section className='panel'><h2>الخط الزمني</h2><div className='timeline'>{item.timeline?.map((row)=><div key={row.id}><b>{row.action}</b><span>{row.actor}</span><time>{new Date(row.happened_at).toLocaleString('ar-SA')}</time></div>)}</div></section></div>}
