import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import api from '../../api/client';

export default function RequestFormPage(){const navigate=useNavigate();const[form,setForm]=useState({title:'',type:'campaign',source:'internal',priority:'medium',brief:{description:''}});const[error,setError]=useState('');
 const submit=async(e)=>{e.preventDefault();try{const{data}=await api.post('/api/v1/requests',form);navigate(`/requests/${data.id}`);}catch(err){setError(err.response?.data?.message||'تعذر إنشاء الطلب');}};
 return <div className='entity-page'><div className='page-heading'><div><p>الطلبات</p><h1>طلب حملة جديد</h1><span>أنشئ الطلب وأرسله إلى فريق التنفيذ.</span></div></div><form className='entity-form' onSubmit={submit}>{error&&<div className='form-alert'>{error}</div>}<div className='form-grid'><label>عنوان الطلب<input value={form.title} onChange={(e)=>setForm({...form,title:e.target.value})} required/></label><label>النوع<select value={form.type} onChange={(e)=>setForm({...form,type:e.target.value})}><option value='campaign'>حملة</option><option value='content'>محتوى</option><option value='booking'>حجز مؤثر</option></select></label><label>المصدر<select value={form.source} onChange={(e)=>setForm({...form,source:e.target.value})}><option value='internal'>داخلي</option><option value='portal'>بوابة العميل</option></select></label><label>الأولوية<select value={form.priority} onChange={(e)=>setForm({...form,priority:e.target.value})}><option value='low'>منخفضة</option><option value='medium'>متوسطة</option><option value='high'>عالية</option><option value='urgent'>عاجلة</option></select></label><label>التفاصيل<textarea value={form.brief.description} onChange={(e)=>setForm({...form,brief:{description:e.target.value}})}/></label></div><div className='form-actions'><button type='button' onClick={()=>navigate('/requests')}>إلغاء</button><button className='primary'>إنشاء الطلب</button></div></form></div>}
