import{useState}from'react';import api from '../../api/client';
export default function ApprovalPage({title='اعتماد الترشيحات'}){const[token,setToken]=useState('');const[nomination,setNomination]=useState('');const[decision,setDecision]=useState('approved');const[notes,setNotes]=useState('');const[msg,setMsg]=useState('');const submit=async(e)=>{e.preventDefault();try{await api.post(`/api/v1/portal/nominations/${nomination}/decision`,{decision,notes},{headers:{'X-Portal-Token':token}});setMsg('تم تسجيل القرار بنجاح.')}catch(err){setMsg(err.response?.data?.message||'تعذر تسجيل القرار')}};return <main className='portal-page'><section className='portal-login approval-card'><h1>{title}</h1><p>استخدم رمز بوابة العميل ورقم الترشيح لتسجيل القرار.</p>{msg&&<div className={msg.includes('بنجاح')?'success-alert':'form-alert'}>{msg}</div>}<form onSubmit={submit}><input value={token} onChange={(e)=>setToken(e.target.value)} placeholder='رمز البوابة' dir='ltr' required/><input type='number' value={nomination} onChange={(e)=>setNomination(e.target.value)} placeholder='رقم الترشيح' required/><select value={decision} onChange={(e)=>setDecision(e.target.value)}><option value='approved'>موافقة</option><option value='rejected'>رفض</option><option value='held'>تعليق</option></select><textarea value={notes} onChange={(e)=>setNotes(e.target.value)} placeholder='ملاحظات القرار'/><button>حفظ القرار</button></form></section></main>}
